# Deployment Screenshots

Add real screenshots to this folder after running the deployment.

Use these filenames so the project documentation stays consistent:

```text
terraform-plan.png
terraform-apply-success.png
aws-vpc-resources.png
ec2-instance-overview.png
nginx-browser-test.png
```

Do not add blank placeholder images. Only commit screenshots captured from the actual Terraform and AWS deployment.
